{
  "extends": "./configs/tsconfig.base.json",
  "include": ["packages", "scripts"],
  "exclude": ["**/node_modules"],

  /* when using ts-node, override the import/export method of module to commonjs method. */
  "ts-node": {
    "compilerOptions": {
      "module": "commonjs"
    }
  }
}